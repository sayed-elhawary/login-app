# Project identification
sonar.projectKey=login-stack
sonar.projectName=Login Stack
sonar.projectVersion=1.0

# Source directories (تأكد من أسماء المجلدات لديك)
sonar.sources=backend,frontend

# Encoding
sonar.sourceEncoding=UTF-8

# Ignore unnecessary files
sonar.exclusions=**/node_modules/**,**/dist/**,**/build/**

# Optional: Include test files if you have any
# sonar.test.inclusions=**/*.spec.js,**/*.test.js

# Optional: Add coverage report paths if you're generating them
# sonar.javascript.lcov.reportPaths=backend/coverage/lcov.info,frontend/coverage/lcov.info
